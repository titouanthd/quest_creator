<form method="post" action="{{ path('app_front_world_delete', {'id': world.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ world.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
