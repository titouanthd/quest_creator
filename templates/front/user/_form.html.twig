{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-primary">
        <i class="bi bi-save me-1"></i>
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
